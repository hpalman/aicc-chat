# 고객 상담사 연결 자동 배정 기능 (순환 참조 해결)

> **작성일**: 2026-02-07 05:42:52  
> **목적**: 고객이 "상담사 연결" 버튼 클릭 시 대기 중인 상담사를 자동으로 배정하여 즉시 채팅 가능하도록 개선  
> **범위**: AgentAssignmentService (신규), MiChatRoutingStrategy, ChatRoutingConfig

---

## 📋 목차

1. [개요](#개요)
2. [순환 참조 문제 및 해결](#순환-참조-문제-및-해결)
3. [변경 사항](#변경-사항)
4. [동작 흐름](#동작-흐름)
5. [테스트 시나리오](#테스트-시나리오)
6. [주의사항](#주의사항)

---

## 개요

### 기존 문제점
- 고객이 "상담사 연결" 버튼 클릭 시 `WAITING` 상태로만 전환
- 상담사가 수동으로 방을 선택하여 배정해야 함
- 고객은 상담사가 배정될 때까지 대기해야 함

### 개선 후
- 고객이 "상담사 연결" 버튼 클릭 시 **대기 중인 상담사를 자동으로 찾아 배정**
- 자동 배정 성공 시 즉시 `AGENT` 모드로 전환하여 채팅 시작 가능
- 자동 배정 실패 시 기존처럼 `WAITING` 모드로 전환

---

## 순환 참조 문제 및 해결

### 문제 발생

**이전 구조**:
```
MiChatRoutingStrategy
    ↓ (의존)
ChatAgentService
    ↓ (의존)
ChatRoutingStrategy (인터페이스)
    ↑ (구현)
MiChatRoutingStrategy  ← 순환 참조! ❌
```

**에러**:
```
The dependencies of some of the beans in the application context form a cycle
```

---

### 해결 방법

**별도 서비스 분리**: `AgentAssignmentService` 생성

**새로운 구조**:
```
MiChatRoutingStrategy
    ↓ (의존)
AgentAssignmentService (새로 생성) ← 순환 참조 없음! ✅
    ↓ (의존)
RoomRepository, MessageBroker, RoomUpdateBroadcaster

ChatAgentService
    ↓ (의존)
ChatRoutingStrategy (순환 참조 없음)
```

---

## 변경 사항

### 1. AgentAssignmentService.java (신규 생성) ⭐

**파일 경로**: `src/main/java/aicc/chat/service/AgentAssignmentService.java`

#### 1.1 대기 중인 상담원 조회 메서드

```java
/**
 * 대기 중인 상담원 중 첫 번째 상담원 조회
 * @return 상담원 ID, 없으면 null
 */
public String findWaitingAgent() {
    Set<String> onlineAgentKeys = redisTemplate.keys(Constants.USER_AGENT_KEY + ":*");
    
    if (onlineAgentKeys != null) {
        for (String key : onlineAgentKeys) {
            String agentId = key.substring((Constants.USER_AGENT_KEY + ":").length());
            
            // Hash에서 agentStatus 조회
            Object agentStatusObj = redisTemplate.opsForHash().get(key, "agentStatus");
            String agentStatus = agentStatusObj != null ? agentStatusObj.toString() : null;
            
            // agentStatus가 WAITING인 경우 반환
            if ("WAITING".equals(agentStatus)) {
                log.info("▶ Found WAITING agent: {}", agentId);
                return agentId;
            }
        }
    }
    
    log.info("▶ No WAITING agent found");
    return null;
}
```

**기능**:
- Redis의 `chat:user-agent:*` 키들을 조회
- `agentStatus`가 `WAITING`인 첫 번째 상담원 반환
- 없으면 `null` 반환

---

#### 1.2 자동 배정 메서드

```java
/**
 * 대기 중인 상담원을 자동으로 배정
 * @param roomId 채팅방 ID
 * @return 배정 성공 여부
 */
public boolean autoAssignWaitingAgent(String roomId) {
    log.info("▼ autoAssignWaitingAgent S. roomId:{}", roomId);
    
    // 1. 대기 중인 상담원 조회
    String agentId = findWaitingAgent();
    if (agentId == null) {
        log.info("▶ No WAITING agent available for auto-assignment");
        return false;
    }
    
    // 2. 상담원 userName 조회
    String agentKey = Constants.USER_AGENT_KEY + ":" + agentId;
    Object userNameObj = redisTemplate.opsForHash().get(agentKey, "userName");
    String agentName = userNameObj != null ? userNameObj.toString() : agentId;
    
    log.info("▶ Auto-assigning WAITING agent: {} ({}) to room: {}", agentName, agentId, roomId);
    
    // 3. 상담원 배정 시도
    boolean success = roomRepository.assignAgent(roomId, agentId);
    if (success) {
        // 4. 상담원 상태를 WORKING으로 변경
        setAgentStatus(agentId, "WORKING");
        
        // 5. 연결 알림 메시지 발송
        ChatMessage notice = ChatMessage.builder()
                .roomId(roomId)
                .sender("System")
                .senderRole(UserRole.SYSTEM)
                .message(agentName + " 상담원과 연결되었습니다.")
                .type(MessageType.TALK)
                .timestamp(LocalDateTime.now())
                .build();
        messageBroker.publish(notice);
        
        // 6. 방 목록 브로드캐스트
        roomUpdateBroadcaster.broadcastRoomList();
        
        log.info("▲ autoAssignWaitingAgent E. Successfully assigned agent {} to room {}", agentName, roomId);
    } else {
        log.warn("▲ autoAssignWaitingAgent E. Failed to assign agent {} to room {}", agentName, roomId);
    }
    
    return success;
}
```

**기능**:
1. 대기 중인 상담원 조회 (`findWaitingAgent()`)
2. 상담원 `userName` 조회
3. `roomRepository.assignAgent()` 호출하여 배정 시도
4. 배정 성공 시:
   - 상담원 상태를 `WORKING`으로 변경
   - 연결 알림 메시지 발송
   - 방 목록 브로드캐스트
5. 배정 성공/실패 여부 반환

---

#### 1.3 상담원 상태 변경 메서드

```java
/**
 * 상담원 상태 변경
 * @param agentId 상담원 ID
 * @param status 상태 (WAITING, WORKING)
 */
private void setAgentStatus(String agentId, String status) {
    String agentKey = Constants.USER_AGENT_KEY + ":" + agentId;
    
    Boolean exists = redisTemplate.hasKey(agentKey);
    if (!exists) {
        log.warn("▶ Agent key not found: {}", agentKey);
        return;
    }
    
    Object currentStatusObj = redisTemplate.opsForHash().get(agentKey, "agentStatus");
    String currentStatus = currentStatusObj != null ? currentStatusObj.toString() : null;
    
    if (!status.equals(currentStatus)) {
        redisTemplate.opsForHash().put(agentKey, "agentStatus", status);
        log.info("▶ Agent {} status changed: {} -> {}", agentId, currentStatus, status);
        
        // 상담원 상태 변경 알림 브로드캐스트
        ChatMessage statusMessage = ChatMessage.builder()
            .roomId("SYSTEM_BROADCAST")
            .sender("System")
            .senderRole(UserRole.SYSTEM)
            .message("AGENT_STATUS")
            .type(MessageType.SYSTEM)
            .timestamp(LocalDateTime.now())
            .build();
        messageBroker.publish(statusMessage);
    }
}
```

---

### 2. MiChatRoutingStrategy.java 수정

#### 2.1 AgentAssignmentService 의존성 추가

**위치**: `src/main/java/aicc/chat/service/impl/MiChatRoutingStrategy.java`

```java
@Slf4j
@RequiredArgsConstructor
public class MiChatRoutingStrategy implements ChatRoutingStrategy {

    private final MessageBroker messageBroker;
    private final ChatBot chatBot;
    private final RoomRepository roomRepository;
    private final RoomUpdateBroadcaster roomUpdateBroadcaster;
    private final ChatHistoryService chatHistoryService;
    private final ChatSessionService chatSessionService;
    private final AgentAssignmentService agentAssignmentService;  // ✅ 추가 (순환 참조 해결)
}
```

---

#### 2.2 HANDOFF 처리 로직 수정

**변경 전**:
```java
// 2. 상담원 연결 요청(HANDOFF)인 경우
if (MessageType.HANDOFF.equals(message.getType())) {
    // @TODO: 대기중인 상담원에게 자동 연결 처리 필요
    
    switchToAgentMode(roomId);
    return;
}
```

**변경 후**:
```java
// 2. 상담원 연결 요청(HANDOFF)인 경우
if (MessageType.HANDOFF.equals(message.getType())) {
    log.info("▶ HANDOFF request received for room: {}", roomId);
    
    // 1. 대기 중인 상담원 자동 배정 시도
    boolean autoAssigned = agentAssignmentService.autoAssignWaitingAgent(roomId);
    
    if (autoAssigned) {
        // 자동 배정 성공: AGENT 모드로 전환
        log.info("▶ Auto-assignment successful, switching to AGENT mode");
        roomRepository.setRoutingMode(roomId, "AGENT");
        roomUpdateBroadcaster.broadcastRoomList();
    } else {
        // 자동 배정 실패: WAITING 모드로 전환 (기존 로직)
        log.info("▶ Auto-assignment failed, switching to WAITING mode");
        switchToAgentMode(roomId);
    }
    return;
}
```

**변경 내용**:
- `agentAssignmentService.autoAssignWaitingAgent()` 호출
- 자동 배정 성공 시 `AGENT` 모드로 즉시 전환
- 자동 배정 실패 시 기존 로직(`WAITING` 모드) 유지

---

### 3. ChatRoutingConfig.java 수정

**위치**: `src/main/java/aicc/chat/config/ChatRoutingConfig.java`

**변경 전**:
```java
@Bean
public ChatRoutingStrategy dynamicRoutingStrategy(
        MessageBroker messageBroker,
        ChatBot chatBot,
        RoomRepository roomRepository,
        RoomUpdateBroadcaster roomUpdateBroadcaster,
        ChatHistoryService chatHistoryService,
        ChatSessionService chatSessionService) {
    
    MiChatRoutingStrategy miChat = new MiChatRoutingStrategy(
            messageBroker, chatBot, roomRepository, roomUpdateBroadcaster,
            chatHistoryService, chatSessionService);
    // ...
}
```

**변경 후**:
```java
@Bean
public ChatRoutingStrategy dynamicRoutingStrategy(
        MessageBroker messageBroker,
        ChatBot chatBot,
        RoomRepository roomRepository,
        RoomUpdateBroadcaster roomUpdateBroadcaster,
        ChatHistoryService chatHistoryService,
        ChatSessionService chatSessionService,
        AgentAssignmentService agentAssignmentService) {  // ✅ 추가
    
    MiChatRoutingStrategy miChat = new MiChatRoutingStrategy(
            messageBroker, chatBot, roomRepository, roomUpdateBroadcaster,
            chatHistoryService, chatSessionService, agentAssignmentService);  // ✅ 주입
    // ...
}
```

**변경 내용**:
- `AgentAssignmentService` 파라미터 추가
- `MiChatRoutingStrategy` 생성자에 `agentAssignmentService` 주입

---

## 동작 흐름

### 시나리오 1: 자동 배정 성공 ✅

```
1. 고객이 "상담사 연결" 버튼 클릭
   ↓
2. WebSocket으로 HANDOFF 메시지 전송
   type: 'HANDOFF'
   roomId: 'room-xxx'
   ↓
3. MiChatRoutingStrategy.handleMessage() 수신
   log: "▶ HANDOFF request received for room: room-xxx"
   ↓
4. agentAssignmentService.autoAssignWaitingAgent(roomId) 호출
   ↓
5. findWaitingAgent()로 대기 중인 상담원 조회
   - Redis: chat:user-agent:* 키들 조회
   - agentStatus == "WAITING"인 상담원 찾기
   - 발견: agent01
   log: "▶ Found WAITING agent: agent01"
   ↓
6. 상담원 userName 조회
   - Redis Hash: chat:user-agent:agent01 > userName
   - 결과: "김상담"
   ↓
7. roomRepository.assignAgent(roomId, agentId) 호출
   - Redis Hash: chat:room-info:{roomId} > assignedAgent = "agent01"
   - Redis Hash: chat:room-info:{roomId} > routingMode = "AGENT"
   - putIfAbsent()로 원자적 배정 보장
   - 성공 반환
   ↓
8. setAgentStatus(agent01, "WORKING") 호출
   - Redis Hash: chat:user-agent:agent01 > agentStatus = "WORKING"
   log: "▶ Agent agent01 status changed: WAITING -> WORKING"
   ↓
9. 연결 알림 메시지 발송
   - messageBroker.publish()
   - message: "김상담 상담원과 연결되었습니다."
   - Redis Pub/Sub → 모든 서버 → /topic/room/{roomId}
   ↓
10. roomRepository.setRoutingMode(roomId, "AGENT")
    - 방 상태를 AGENT로 설정
    ↓
11. roomUpdateBroadcaster.broadcastRoomList()
    - /topic/rooms 채널로 방 목록 브로드캐스트
    ↓
12. 고객 화면 업데이트
    - "김상담 상담원과 연결되었습니다." 메시지 표시
    - 버튼: "상담원 연결됨" (비활성화)
    ↓
13. 상담원 화면 업데이트
    - 방 목록에 room-xxx 표시
    - 상태: AGENT
    - assignedAgent: agent01
    ↓
14. 고객과 상담원이 즉시 채팅 시작 가능 ✅
```

---

### 시나리오 2: 자동 배정 실패 (대기 중인 상담원 없음) ⏳

```
1. 고객이 "상담사 연결" 버튼 클릭
   ↓
2. WebSocket으로 HANDOFF 메시지 전송
   ↓
3. MiChatRoutingStrategy.handleMessage() 수신
   log: "▶ HANDOFF request received for room: room-xxx"
   ↓
4. agentAssignmentService.autoAssignWaitingAgent(roomId) 호출
   ↓
5. findWaitingAgent()로 대기 중인 상담원 조회
   - WAITING 상태인 상담원 없음
   - null 반환
   log: "▶ No WAITING agent found"
   ↓
6. autoAssignWaitingAgent() false 반환
   log: "▶ No WAITING agent available for auto-assignment"
   ↓
7. switchToAgentMode(roomId) 호출 (기존 로직)
   - routingMode를 "WAITING"으로 설정
   - "상담원 연결을 요청하였습니다..." 메시지 발송
   log: "▶ Auto-assignment failed, switching to WAITING mode"
   ↓
8. 고객 화면 업데이트
   - "상담원 연결을 요청하였습니다. 상담원이 연결될 때까지 잠시만 기다려 주세요." 메시지 표시
   - 버튼: "상담원 연결 중" (비활성화)
   ↓
9. 고객은 상담원이 수동으로 배정할 때까지 대기 ⏳
```

---

### 시나리오 3: 동시 요청 처리 (경쟁 조건)

**전제 조건**:
- 상담원 A만 `WAITING` 상태
- 고객 B, C가 동시에 "상담사 연결" 클릭

**처리 과정**:
```
[고객 B 요청]
1. findWaitingAgent() → agent01 반환
2. assignAgent(roomB, agent01) → putIfAbsent() 성공 ✅
3. setAgentStatus(agent01, "WORKING")
4. 연결 알림 발송: "김상담 상담원과 연결되었습니다."
5. 고객 B와 상담원 A 채팅 시작 ✅

[고객 C 요청] (거의 동시)
1. findWaitingAgent() → null 반환 (이미 WORKING)
2. autoAssignWaitingAgent() → false 반환
3. switchToAgentMode(roomC) → WAITING 모드
4. 고객 C는 대기 상태 ⏳
```

**원자성 보장**:
- `roomRepository.assignAgent()`는 `putIfAbsent()`를 사용하여 원자적 배정 보장
- 동시 요청 시 첫 번째 요청만 성공

---

## 테스트 시나리오

### 테스트 1: 순환 참조 해결 확인 ✅

```bash
# 애플리케이션 시작
./gradlew bootRun

# 확인 사항:
# ✅ 순환 참조 에러 없이 정상 시작
# ✅ Bean 생성 로그 확인:
#    - "▼ dynamicRoutingStrategy"
#    - "▼ miChatRoutingStrategy"
```

---

### 테스트 2: 자동 배정 기능 테스트 ✅

**단계**:
```
1. 상담원 로그인
   - http://localhost:28070/chat-agent.html
   - ID: agent01, PW: agent01
   - "대기" 버튼 클릭
   - Redis 확인: chat:user-agent:agent01 > agentStatus = "WAITING"

2. 고객 로그인
   - http://localhost:28070/chat-customer.html
   - 이름: 홍길동
   - 상담 시작

3. 고객이 "상담사 연결" 버튼 클릭
   ↓
4. 서버 로그 확인:
   [MiChatRoutingStrategy] ▼ handleMessage. roomId:room-xxx,message:...
   [MiChatRoutingStrategy] ▶ HANDOFF request received for room: room-xxx
   [AgentAssignmentService] ▼ autoAssignWaitingAgent S. roomId:room-xxx
   [AgentAssignmentService] ▶ Found WAITING agent: agent01
   [AgentAssignmentService] ▶ Auto-assigning WAITING agent: 김상담 (agent01) to room: room-xxx
   [RedisRoomRepository] ▶▶▶ roomId:room-xxx,agentId:agent01
   [AgentAssignmentService] ▶ Agent agent01 status changed: WAITING -> WORKING
   [AgentAssignmentService] ▲ autoAssignWaitingAgent E. Successfully assigned agent 김상담 to room room-xxx
   [MiChatRoutingStrategy] ▶ Auto-assignment successful, switching to AGENT mode
   ↓
5. 고객 화면 확인:
   - "김상담 상담원과 연결되었습니다." 메시지 표시
   - 버튼: "상담원 연결됨" (비활성화)
   ↓
6. 상담원 화면 확인:
   - 방 목록에 room-xxx 표시
   - 상태: AGENT
   - assignedAgent: agent01
   ↓
7. 채팅 테스트:
   - 고객: "안녕하세요" 전송
   - 상담원: 메시지 수신 및 응답 가능 ✅
```

---

### 테스트 3: 대기 중인 상담원 없을 때 ⏳

**단계**:
```
1. 모든 상담원 로그아웃 또는 WORKING 상태
   ↓
2. 고객이 "상담사 연결" 버튼 클릭
   ↓
3. 서버 로그 확인:
   [AgentAssignmentService] ▶ No WAITING agent found
   [AgentAssignmentService] ▶ No WAITING agent available for auto-assignment
   [MiChatRoutingStrategy] ▶ Auto-assignment failed, switching to WAITING mode
   [MiChatRoutingStrategy] ▼ switchToAgentMode S. roomId:room-xxx WAITING
   ↓
4. 고객 화면 확인:
   - "상담원 연결을 요청하였습니다. 상담원이 연결될 때까지 잠시만 기다려 주세요." 메시지 표시
   - 버튼: "상담원 연결 중" (비활성화)
   ↓
5. 상담원이 로그인 후 "대기" 버튼 클릭
   ↓
6. 상담원이 수동으로 방 배정
   ↓
7. 고객과 상담원 채팅 시작 ⏳
```

---

## Redis 키 구조

### 상담원 정보 (Hash)

**키**: `chat:user-agent:{agentId}`

**Hash 필드**:
| 필드 | 타입 | 설명 | 예시 |
|------|------|------|------|
| `userName` | String | 상담원 이름 | "김상담" |
| `userId` | String | 상담원 ID | "agent01" |
| `agentStatus` | String | 상담원 상태 | `WAITING` 또는 `WORKING` |
| `loginTime` | String | 로그인 시간 | "2026-02-07T05:25:32" |
| `lastHeartbeat` | String | 마지막 하트비트 | "2026-02-07T05:30:00" |

**agentStatus 값**:
- `WAITING`: 대기 중 (자동 배정 가능)
- `WORKING`: 업무 중 (자동 배정 불가)

---

### 방 정보 (Hash)

**키**: `chat:room-info:{roomId}`

**Hash 필드**:
| 필드 | 타입 | 설명 | 예시 |
|------|------|------|------|
| `routingMode` | String | 라우팅 모드 | `BOT`, `WAITING`, `AGENT`, `CLOSED` |
| `assignedAgent` | String | 배정된 상담원 ID | "agent01" |
| `lastActivity` | String | 마지막 활동 시간 | "1738887932000" |

---

## 주의사항

### 1. 상담원 상태 관리

- 자동 배정 성공 시 상담원 상태를 `WORKING`으로 변경
- 상담 종료 시 상담원 상태를 다시 `WAITING`으로 변경해야 함 (별도 구현 필요)
- 상담원이 수동으로 "대기 종료" 버튼 클릭 시에도 상태 변경 필요

### 2. 경쟁 조건 (Race Condition)

- 여러 고객이 동시에 요청할 경우 첫 번째 요청만 성공
- `roomRepository.assignAgent()`의 `putIfAbsent()`로 원자성 보장
- 실패한 요청은 `WAITING` 모드로 전환되어 수동 배정 대기

### 3. 상담원 부재 시

- 대기 중인 상담원이 없으면 기존 로직(`WAITING` 모드) 유지
- 상담원이 로그인 후 "대기" 버튼 클릭 시 수동 배정 가능

### 4. 순환 참조 방지

**원칙**:
- 서비스 계층 간 순환 참조 발생 시 별도 서비스로 분리
- 단일 책임 원칙(SRP) 준수
- 의존성 방향을 명확하게 유지

**예시**:
```
✅ 올바른 의존성:
   Controller → Service → Repository

❌ 잘못된 의존성:
   ServiceA ↔ ServiceB (순환 참조)
```

---

## 변경된 파일 목록

### 신규 생성 (1개)

1. ✅ `src/main/java/aicc/chat/service/AgentAssignmentService.java`
   - 대기 중인 상담원 조회
   - 자동 배정 로직
   - 상담원 상태 관리

### 수정 (3개)

2. ✅ `src/main/java/aicc/chat/service/impl/MiChatRoutingStrategy.java`
   - `AgentAssignmentService` 의존성 추가
   - `HANDOFF` 처리 로직 수정 (자동 배정 추가)

3. ✅ `src/main/java/aicc/chat/config/ChatRoutingConfig.java`
   - `AgentAssignmentService` 주입 추가
   - `dynamicRoutingStrategy` Bean 생성 시 주입
   - `miChatRoutingStrategy` Bean 생성 시 주입

4. ✅ `src/main/java/aicc/chat/service/ChatAgentService.java`
   - 중복 메서드 제거 (순환 참조 해결)

---

## 🎉 완료!

고객이 "상담사 연결" 버튼 클릭 시 **대기 중인 상담사를 자동으로 배정**하여 즉시 채팅을 시작할 수 있습니다!

**주요 개선사항**:
- ✅ 순환 참조 해결 (`AgentAssignmentService` 분리)
- ✅ 대기 중인 상담원 자동 조회 및 배정
- ✅ 자동 배정 성공 시 즉시 AGENT 모드로 전환
- ✅ 자동 배정 실패 시 기존 WAITING 모드 유지
- ✅ 상담원 상태 자동 관리 (WAITING → WORKING)
- ✅ 원자적 배정 보장 (putIfAbsent)
- ✅ 단일 책임 원칙 준수

**테스트 체크리스트**:
```
□ 애플리케이션 정상 시작 (순환 참조 에러 없음)
□ 대기 중인 상담원 자동 배정 성공
□ 대기 중인 상담원 없을 때 WAITING 모드 전환
□ 동시 요청 시 첫 번째 요청만 성공
□ 상담원 상태 자동 변경 (WAITING → WORKING)
□ 연결 알림 메시지 발송 확인
□ 방 목록 브로드캐스트 확인
□ 고객과 상담원 채팅 가능
```

---

## 관련 문서

- `doc/ai-qa/20260207054252_순환참조_해결_AgentAssignmentService_분리.md` - 순환 참조 해결 상세 가이드
- `doc/ai-qa/20260204174149_상담원_가용성_체크_WAITING_필터링_개선.md` - 상담원 가용성 체크 로직
- `doc/ai-qa/AGENT_LOGIN_NOTIFICATION.md` - 상담원 로그인 알림 기능
